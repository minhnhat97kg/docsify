name: Content Pipeline

on:
  push:
    branches: [main, master]
    paths:
      - "content/**"
      - "scripts/processor.py"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-index:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install python-frontmatter PyYAML

      - name: Run content processor
        run: python scripts/processor.py

      - name: Commit updated index.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if git diff --quiet public/index.json 2>/dev/null && \
             git ls-files --error-unmatch public/index.json 2>/dev/null; then
            echo "No changes to index.json — skipping commit."
          else
            git add public/index.json
            git commit -m "chore: rebuild index.json [skip ci]"
            git push
          fi
